{
  "name": "@biblioteca/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@biblioteca/domain": "*"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "typescript": "^5.3.3",
    "ts-node-dev": "^2.0.0"
  }
}
